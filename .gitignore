.build/
AudioTune.app/
*.xcodeproj
.DS_Store
audiotune.log
docs/*.iconset/
# Intermediate icon frames from tools/makeicon.swift — the committed
# bundle/AppIcon.icns is what the build actually uses.
build/
